export const mobile = window.innerWidth < 640;
export const tablet = window.innerWidth >= 640 && window.innerWidth < 1280;
export const full = window.innerWidth >= 1500;
export const laptop = window.innerWidth >= 1280 && window.innerWidth < 1500;
